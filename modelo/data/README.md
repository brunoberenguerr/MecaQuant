Esta pasta é populada rodando `python scripts/01_build_universe.py` — os
caches de preços e universo (`raw/`, `interim/`, `processed/`) não são
versionados (ver `.gitignore`), pois são grandes e inteiramente reprodutíveis.
